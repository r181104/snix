super + Return
alacritty

super + @space
rofi -show drun

super + Escape
pkill -USR1 -x sxhkd

super + alt + {q, r}
bspc {quit, wm -r}

super + {_,shift + }q
bspc node -{c, k}

super + shift + t
bspc desktop -l next
